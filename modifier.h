#include <algorithm>
#include <string>

/* language games */

class Modifier {
public:
    std::string palindrome(std::string&);
    std::string pigLatin(std::string&);
    std::string reverse(std::string&);
    std::string shuffle(std::string&);
};